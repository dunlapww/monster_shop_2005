<%= form_for(@user, url: "/passwords", :method=>'patch', :id => 'user_new') do |f| %>
  <%= f.label :password %>
  <%= f.password_field :password%><br>
  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation %><br>
  <%= f.submit "Submit" %>
<% end %>
